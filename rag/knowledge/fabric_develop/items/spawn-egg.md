# 刷怪蛋

你必须先了解如何创建一个物品，然后才能举一反三，转变成刷怪蛋。

本文还引用了创建你的第一个实体中的迷你傀儡实体。 如果你没有按照那个教程操作，你可以使用诸如 `EntityType.FROG` 的普通实体，而不必是 `ModEntityTypes.MINI_GOLEM`。

刷怪蛋是一种特殊物品，使用后会生成相应的生物。 你可以通过向物品类中的 `register` 方法传递 `SpawnEggItem::new` 来注册一个。
